# Retries and backoff

!!! note "Stub page"

    Placeholder created by KAN-483 (docs skeleton). The cookbook card fills this
    in: a flaky task under capped exponential backoff with full jitter, and what `effect_safety` does to a retryable `side_effect=True` task.
